import type { PlanetContent } from './types'
import { PLANETS_BY_ID } from './planets'

// Placeholder — real concepts/quizzes/facts/achievements authored in Phase 3 (spec Section 15).
export const aiPlanetContent: PlanetContent = {
  meta: PLANETS_BY_ID.ai,
  concepts: [],
  quizzes: [],
  facts: [],
  achievements: [],
}
